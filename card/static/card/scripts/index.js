"use strict";

// Maybe use babel to transcribe from ES6 later
var React = require ('react');
var ReactDOM = require ('react-dom');

class IndexTabView extends React.Component {
	render () {
        const courseNames = props.data;
        const listItems = courseName.map ( );
		return ;//<ul>{listItems}</ul>);
	}
}

//ReactDOM.render( <IndexTabView data={data} />, document.getElementById ('root') );
